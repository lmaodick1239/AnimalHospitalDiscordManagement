import type { Router } from "express";

export function registerStartRoutes(_router: Router): void {
  // Start routing is composed by createWebApp so authentication and shared dependencies stay centralized.
}
