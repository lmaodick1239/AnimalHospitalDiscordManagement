import type { Router } from "express";

export function registerInstanceRoutes(_router: Router): void {
  // Instance routing is composed by createWebApp with the shared auth and report dependencies.
}
